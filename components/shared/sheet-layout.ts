/** Narrow side sheet — default `sm:max-w-sm` (tasks, template properties). */
export const sheetContentClass = "overflow-y-auto";

/** Medium forms — settings, Stripe connect. */
export const sheetContentMediumClass = "overflow-y-auto sm:max-w-lg";

/** Wide forms — catalog, CRM. */
export const sheetContentWideClass = "overflow-y-auto sm:max-w-2xl";

/** Form body under SheetHeader (UI kit Edit To-Do pattern). */
export const sheetFormClass = "space-y-6 p-4 pt-0";

/** Tab panels aligned to form vertical rhythm. */
export const sheetTabsClass = "gap-6";

/** Primary actions row at the bottom of sheet forms. */
export const sheetActionsRowClass = "flex items-center justify-between gap-2";

export const sheetActionsEndClass = "flex items-center justify-end gap-2";
