/**
 * Fixed agreement modal / document chrome — intentionally separate from
 * `.proposal-rich-text` content typography.
 */

/** Hero document title (Services Agreement) — not editable rich text. */
export const AGREEMENT_DOCUMENT_TITLE_CLASSES =
  "font-serif text-4xl font-semibold leading-tight tracking-tight text-foreground sm:text-5xl";

/** Compact title in the modal toolbar. */
export const AGREEMENT_MODAL_HEADER_TITLE_CLASSES =
  "truncate text-sm font-semibold tracking-tight sm:text-base";

/** Side nav parent link. */
export const AGREEMENT_NAV_LINK_CLASSES =
  "h-auto w-full justify-start px-3 py-2.5 font-medium";

/** Side nav nested section link. */
export const AGREEMENT_NAV_CHILD_LINK_CLASSES =
  "h-auto w-full justify-start py-2 pl-4 pr-3 font-normal text-muted-foreground hover:text-foreground";
