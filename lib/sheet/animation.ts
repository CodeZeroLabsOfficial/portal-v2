/** Sheet open/close duration in ms — keep in sync with `SHEET_ANIMATION_DURATION_CLASSES`. */
export const SHEET_ANIMATION_MS = 300;

/** Tailwind duration classes for Radix `data-[state=open|closed]` sheet animations. */
export const SHEET_ANIMATION_DURATION_CLASSES =
  "data-[state=closed]:duration-300 data-[state=open]:duration-300";
